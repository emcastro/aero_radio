version: 1
disable_existing_loggers: false
formatters:
  default:
    "()": log_config.MicrosecondFormatter
    fmt: "%(asctime)s [%(levelname)s] [%(name)s] %(message)s"
    use_colors: false
  access:
    "()": log_config.MicrosecondAccessFormatter
    fmt: '%(asctime)s [%(levelname)s] [http] %(client_addr)s - "%(request_line)s" %(status_code)s'
handlers:
  default:
    formatter: default
    class: logging.StreamHandler
    stream: ext://sys.stderr
  access:
    formatter: access
    class: logging.StreamHandler
    stream: ext://sys.stdout
loggers:
  uvicorn:
    handlers: [default]
    level: INFO
    propagate: false
  uvicorn.error:
    level: INFO
  uvicorn.access:
    handlers: [access]
    level: INFO
    propagate: false
  auth:
    handlers: [default]
    level: INFO
    propagate: false
